from django.contrib import admin
from .models import Producto
# Register your models here.
class ProdAdmin(admin.ModelAdmin):
	class Meta:
		model=Producto
#	list_display = ('nombre', 'Add')


admin.site.register(Producto, ProdAdmin)
